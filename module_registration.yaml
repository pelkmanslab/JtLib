AlignImage:
    handles: handles/AlignImage.handles
    module: '{libpath}/modules/AlignImage.py'
    interpreter: python
    active: true

AlignSegmentation:
    handles: handles/AlignSegmentation.handles
    module: '{libpath}/modules/AlignSegmentation.py'
    interpreter: python
    active: true

IllumCorrectImage:
    handles: handles/IllumCorrectImage.handles
    module: '{libpath}/modules/IllumCorrectImage.py'
    interpreter: python
    active: true

IdentifyCells:
    handles: handles/IdentifyCells.handles
    module: '{libpath}/modules/IdentifyCells.m'
    interpreter: Mscript
    active: true

IdentifyNuclei:
    handles: handles/IdentifyNuclei.handles
    module: '{libpath}/modules/IdentifyNuclei.m'
    interpreter: Mscript
    active: true

LoadImages:
    handles: handles/LoadImages.handles
    module: '{libpath}/modules/LoadImages.py'
    interpreter: python
    active: true

LoadSegmentation:
    handles: handles/LoadSegmentation.handles
    module: '{libpath}/modules/LoadSegmentation.py'
    interpreter: python
    active: true

MeasureAreaShape:
    handles: handles/MeasureAreaShape.handles
    module: '{libpath}/modules/MeasureAreaShape.py'
    interpreter: python
    active: true

MeasureIntensity:
    handles: handles/MeasureIntensity.handles
    module: '{libpath}/modules/MeasureIntensity.py'
    interpreter: python
    active: true
