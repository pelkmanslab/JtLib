hdf5_filename: None

# Fix bad pixels
# ==============

# The module replaces NaN and Inf values by an estimate of the local
# intensities (derived from smoothing the surrounding);
#
# Authors:
#  Thomas Stoeger (function fixNonNumericalValueInImage.m)
#  Markus Herrmann (Jterator module implementation)

input:

  InputImage:
    hdf5_location: /PrimeRawData/DapiImage

output:

  OutputImage:
    hdf5_location: /FixBadPixels/FixedDapiImage
